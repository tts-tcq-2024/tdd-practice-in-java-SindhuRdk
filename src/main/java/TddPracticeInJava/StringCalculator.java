package TddPracticeInJava;

public class StringCalculator {

    public int add(String numbers) {
        if (numbers.isEmpty()) {
            return 0;
        }
        return -1;  // Default return to fail for now
    }
}
